## Surface
Tiny library for creating user interfaces in Pygame applications.  
Contains the following components:
* Buttons
* Checkboxes
* Sliders
* Textboxes

To use each component, the program needs to call the `update()` and `render()`
methods in the game loop.
